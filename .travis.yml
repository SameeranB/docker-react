language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t sameeranbandishti/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sameeranbandishti/docker-react npm run test